<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout android:id="@+id/nav_header"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="170dp"
    android:background="@color/nav_menu_background"
    android:fitsSystemWindows="true">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/nav_img"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_marginVertical="20dp"
        android:layout_marginHorizontal="10dp"
        android:padding="10dp"
        android:scaleType="fitXY"
        app:shapeAppearanceOverlay="@style/circle"
        app:srcCompat="@drawable/cute_fox_sticker"
        app:strokeColor="@color/light_purple"
        app:strokeWidth="4dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@id/nav_img"
        android:orientation="vertical">

        <TextView
            android:id="@+id/nav_username"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:layout_marginHorizontal="10dp"
            android:text="@string/unauthorized_username"
            android:textColor="@color/white"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/nav_email"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:layout_marginHorizontal="10dp"
            android:textColor="#FFFFFF"
            android:text="@string/unauthorized_username" />
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>

